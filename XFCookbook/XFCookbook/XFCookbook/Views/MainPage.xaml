<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
             xmlns:controls="clr-namespace:XFCookbook.Controls;assembly=XFCookbook"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="XFCookbook.Views.MainPage"
             Title="MainPage">
  <StackLayout Padding="10" VerticalOptions="FillAndExpand">
    <Entry Text="{Binding SourceUrl}"/>
    <Button Text="Load" Command="{Binding LoadCommand}"/>
    <controls:ExtendedWebView Source="{Binding WebViewSource}" EvaluateJavascript="{Binding EvaluateJavascript,Mode=OneWayToSource}" VerticalOptions="FillAndExpand"/>
  </StackLayout>
</ContentPage>